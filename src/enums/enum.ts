enum Screen {
    MoviesList = 'moviesList',
    Movie = 'movie',
}

enum Search {
    Title = 'title',
    Genre = 'genre',
}

enum Sort {
    ReleaseDate = 'releaseDate',
    Rating = 'rating',
}

export {Screen, Search, Sort};
